CONFIG_PM=n

CONFIG_MAIN_STACK_SIZE=10240

CONFIG_BOOT_SWAP_SAVE_ENCTLV=n
CONFIG_BOOT_ENCRYPT_IMAGE=n

# Disable all swap modes - we want RAM load instead
CONFIG_BOOT_SWAP_USING_SCRATCH=n
CONFIG_BOOT_SWAP_USING_OFFSET=n
CONFIG_BOOT_SWAP_USING_MOVE=n
CONFIG_BOOT_UPGRADE_ONLY=n

# Disable swap preferences so BOOT_RAM_LOAD is used
CONFIG_BOOT_PREFER_SWAP_OFFSET=n
CONFIG_BOOT_PREFER_SWAP_MOVE=n

# Enable RAM load mode - MCUboot will copy app from external flash to RAM before chainload
CONFIG_BOOT_RAM_LOAD=y
CONFIG_MULTIPLE_EXECUTABLE_RAM_REGIONS=y

CONFIG_BOOT_BOOTSTRAP=n

### mbedTLS has its own heap
# CONFIG_HEAP_MEM_POOL_SIZE is not set

CONFIG_FLASH=y
CONFIG_FLASH_BASE_ADDRESS=0x0
CONFIG_FLASH_LOAD_OFFSET=0x0

### Various Zephyr boards enable features that we don't want.
# CONFIG_BT is not set
# CONFIG_I2C is not set

CONFIG_LOG=y
CONFIG_LOG_MODE_MINIMAL=y
### Ensure Zephyr logging changes don't use more resources
CONFIG_LOG_DEFAULT_LEVEL=3
### Use debug log level to see what's happening
CONFIG_MCUBOOT_LOG_LEVEL_DBG=y
### Decrease footprint by ~4 KB in comparison to CBPRINTF_COMPLETE=y
CONFIG_CBPRINTF_NANO=y
### Use picolibc to reduce flash usage
CONFIG_PICOLIBC=y
### Disable malloc arena because we don't need it
CONFIG_COMMON_LIBC_MALLOC_ARENA_SIZE=0
